'use strict';

const cars = ['Bmw', 'audi', 'kia', 'Mazda', 'mercedes'];

// --- FILTER ---

// const carsFiltred = cars.filter((car) => {
// 	return car.length < 5;
// });
// console.log(carsFiltred);

// --- MAP ---

// const carsFiltred = cars.map((car) => {
// 	return car.toLocaleLowerCase();
// });
// console.log(carsFiltred);

// --- EVERY/SOME ---

// console.log(cars.some((car) => car.length > 5));
// console.log(cars.every((car) => car.length > 5));

// --- REDUCE ---

// const numbers = [10, 3, 21, 5, 74, 9];

// const numberReduce = numbers.reduce((sum, item) => sum + item);
// console.log(numberReduce);

// const carsReduce = cars.reduce((stringCars, item) => {
// 	return `${stringCars}, ${item}`.toLowerCase();
// });
// console.log(carsReduce);
